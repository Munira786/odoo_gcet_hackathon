# HRMS - Human Resource Management System

A comprehensive HRMS solution built with **React (Vite) + Tailwind CSS** frontend and **PHP (Native) + MySQL** backend. 

## 🚀 Features
- **Role-Based Access**: Admin, HR, Employee roles.
- **Authentication**: Secure login with password hashing.
- **Employee Management**: Auto-generated Employee Codes, detailed profiles.
- **Attendance**: Daily Check-in/Check-out with status tracking.
- **Leaves**: Leave request workflow (Apply -> Approve/Reject).
- **Payroll/Salary**: Confidential salary breakdown and net pay calculation.

## 🛠️ Tech Stack
- **Frontend**: React 18, Vite, Tailwind CSS, Lucide Icons, Axios.
- **Backend**: Native PHP 8+, PDO, MySQL.
- **Database**: MySQL (Normalized schema).

## 📂 Project Structure
- `frontend/`: React application source code.
- `backend/`: PHP API endpoints and configuration.
- `database.sql`: Database schema import file.

## ⚙️ Setup Instructions

### 1. Backend Setup (XAMPP)
1.  Copy the `HRMS` folder to `htdocs` (e.g., `C:\xampp\htdocs\HRMS`).
2.  Start **Apache** and **MySQL** in XAMPP Control Panel.
3.  Open `http://localhost/phpmyadmin` and create a database named `hrms_db`.
4.  Import `HRMS/database.sql` into `hrms_db`.
5.  Configure `backend/config/database.php` if your MySQL credentials differ from `root` / `""`.

### 2. Frontend Setup
1.  Open terminal in `HRMS/frontend`.
2.  Install dependencies:
    ```bash
    npm install
    ```
3.  Start development server:
    ```bash
    npm run dev
    ```
4.  Access the app at `http://localhost:5173`.

## 🔑 Default Credentials
**Admin User**
- Email: `admin@hrms.com`
- Password: `admin123` (Note: Ensure the database has the hashed version if manually inserting, the provided SQL seed includes it).

## 📝 Usage
- **Login**: Use credentials to sign in.
- **Dashboard**: View team status.
- **Profile**: Check salary (click "Show Values").
- **Attendance**: Use the button (if implemented in UI) or API to mark attendance.

---
*Generated by Antigravity AI*
